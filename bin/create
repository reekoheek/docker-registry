#!/bin/bash

docker create \
    --name registry \
    --net host \
    --restart always \
    -p 5000:5000 \
    -e STANDALONE=false \
    -e MIRROR_SOURCE=https://registry-1.docker.io \
    -e MIRROR_SOURCE_INDEX=https://index.docker.io \
    -v $PWD/data:/tmp \
    registry

